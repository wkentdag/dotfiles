# Mouse: click to select panes, scroll, resize, and select text.
set -g mouse on

# True color for Vim / Cursor-matching syntax themes in Ghostty.
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"
